#include <bits/stdc++.h>

using namespace std;

typedef vector<int> vi;

int main() {
    // Otimização de input
    ios::sync_with_stdio(false);
    cin.tie(0);

    int t;
    cin >> t;

    while(t--) {
        // Implementation
    }

    return 0;
}