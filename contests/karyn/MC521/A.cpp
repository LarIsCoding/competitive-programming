#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
    int angulo;
    while(cin >> angulo){
    if (angulo%6 == 0) cout << 'Y' << endl;
    else cout << 'N' << endl; }
    return 0;
}
