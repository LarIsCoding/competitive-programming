#include <bits/stdc++.h>

using namespace std;

int main() {
   // Otimização de input
   ios::sync_with_stdio(false);
   cin.tie(0);

   int n, k, aux;
   cin >> n >> k;

   priority_queue<pair<int, int>, vector<pair<int, int> >, greater<pair<int, int> > > notas;
   queue<pair<>>

   for(int i = 0; i < k; i++) {
       cin >> aux;
       notas.push(aux);
   }

   return 0;
}